# 変更履歴 (Changelog)

このプロジェクトは[セマンティックバージョニング](https://semver.org/lang/ja/)に準拠しています。
v0.54.0でバージョン管理をgit/GitHub（[Releases](https://github.com/LightSteps4643/SkyrimJPTranslationSupporter/releases)・タグ）ベースに移行しており、本ファイルもそれ以降を対象としています。

それより前の履歴は開発メモ（非公開）に記録されています。

### v0.59.2 (2026-09-03)

#### 修正

- 本（BOOK）レコードで、本文（body）と短い説明文（description）が入れ替わってDSDファイルに出力されてしまう不具合を修正しました。本系のアイテムを追加するMODで、アイテムの説明文と本文が入れ替わって表示される原因になっていました（[Issue #2](https://github.com/LightSteps4643/SkyrimJPTranslationSupporter/issues/2)でご報告いただきました、ありがとうございました）。
  - 併せて、他の16種類のレコード種別・7種類のネスト構造については、同様の入れ替わりが無いことも確認済みです。

### v0.59.1 (2026-09-02)

#### 変更

- 「簡易名前解決」の既定をOFFに変更しました（当面はローカルLLM翻訳の方が高品質なため）。

#### 修正

- 翻訳詳細ウィンドウで、複数行の訳文セルを編集モードにすると改行が消えて見える不具合を修正しました（保存されるファイル自体は元々正しく、表示だけの問題でした）。
- ローカルLLM翻訳・生成AI翻訳で、モデルが訳文を区切りタグ（`<SJPTS_TARGET>`）で囲んで返した場合に、そのタグが訳文にそのまま残ってしまう不具合を修正しました。

## v0.59.0 (2026-09-01 22:58)

#### 追加

- ローカルLLM翻訳に、モデルの「思考」モードを切り替えるチェックボックスを追加しました（既定でチェック済み＝思考モードOFFが既定で有効）。
  - 思考モードは、対応モデルではONのままだと翻訳に大幅に時間がかかったり失敗しやすくなったりするため、OFF推奨です。
  - Ollama＋`gemma4:26b`の組み合わせで、実際に思考モードを切り替えられることを確認済みです。
  - 思考に対応していない`gemma3:12b`・`qwen2.5:14b-instruct`でも、この設定を送って害が無いことは確認済みですが、それ以外のモデル・サーバーとの組み合わせは未検証です。
- ローカルLLM翻訳・生成AI翻訳（クラウド）の「1回あたりの文字数上限」を、設定ウィンドウでそれぞれ独立して設定できるようにしました。
- 未翻訳候補の抽出時に除外した項目の理由を、専用ログファイル（`PickUpTarget/excluded_candidates.txt`）にまとめて出力するようにしました。あわせて、なぜ除外したのかが一目で分かるよう理由の文言も見直しています。

#### 変更

- 「生成AI連携設定」を独立ウィンドウから設定ウィンドウ内へ統合しました。
- ローカルLLM翻訳・生成AI翻訳のバッチ処理を全体的に見直し、翻訳できない結果を取りこぼしにくくしました。
- ローカルLLM翻訳・生成AI翻訳で、モデルの訳文に日本語が含まれない場合、以前は一律「翻訳失敗」扱いでしたが、意図的に翻訳していない文字列（スクランブル文字列等）である可能性も考慮し、専用タグを付けた上でDSDファイルにもそのまま出力し、後からレビューできるようにしました。

#### 修正

- ローカルLLM翻訳・生成AI翻訳で、翻訳自体は正しいのに未解決のまま残ってしまうことがある不具合を修正しました。
- ローカルLLM翻訳のタイムアウト・連続失敗時の打ち切りを見直し、応答が長くかかるモデルでも早期に諦めすぎないようにしました。
- ウィンドウ下部のボタン列に、操作を繰り返すうちに余分な空白ができてしまうことがある表示不具合を修正しました。

## v0.58.0 (2026-08-31 12:51)

#### 変更

- 配布フォルダの構成を変更しました。起動用の実行ファイル（`SkyrimJPStringPatcherGui.exe`）がフォルダ直下に配置され、直接ダブルクリックで起動できるようになりました（ランチャーの`.bat`ファイルは廃止）。内部処理用のCLI（`SkyrimJPStringPatcher.exe`）は`SkyrimJPStringPatcher`サブフォルダに移動しました。

### v0.57.4 (2026-08-30 15:04)

#### 変更

- 設定ウィンドウのmods/profile/overwrite個別パス設定を分かりやすく整理しました（説明文付きの枠にまとめ、項目名を簡潔化）。
- 設定ウィンドウの「MO2フォルダをロード」ボタンを削除しました（実行しても翻訳状況の一覧には反映されず、混乱を招く恐れがあったため）。MO2の再読み込みはベース画面の「MO2再読込＆初期化」をご利用ください。

#### 修正

- プロファイルフォルダを個別設定した際、実行ログの「プロファイル」表示がMO2側で現在選択中のプロファイル名のままになり、実際に処理したプロファイルと食い違って表示される不具合を修正しました。
- DSDファイル生成の出力ファイル名に実行日時を含めるようにしました。未翻訳分を後日追加で翻訳して再度DSD出力・導入した際、以前の翻訳がMO2の仮想ファイルシステム上で消えてしまう不具合を修正しています。

### v0.57.1 (2026-08-30 13:36)

#### 追加

- MO2の「Paths」設定タブでmods・profiles・overwriteフォルダを標準位置から再配置している場合に対応する、任意の3つのパス上書き設定を追加しました（設定ウィンドウ・CLIとも）。未設定（既定）であれば従来通りMO2インスタンスフォルダから自動的に導出されます。

#### 変更

- 設定ウィンドウのボタンを、単一の「設定を保存」から、一般的なOK（保存して閉じる）／キャンセル（保存せず閉じる）に変更しました。

#### 修正

- MO2インスタンスフォルダ、またはmods/プロファイル/overwriteフォルダのパスが誤っている場合に、原因の分からないクラッシュ（終了コード-532462766）で処理が止まってしまう不具合を修正しました。今後は分かりやすいエラーメッセージとともに、正常に処理を終了します。

### v0.56.2 (2026-08-29 14:39)

#### 修正

- v0.56.0のクロスMOD翻訳資産対応で、バニラのレコードを別物へ改名するオーバーホールMOD（例: パークツリー改変MOD）に対して、古い訳が無条件に適用されてしまう不具合を修正しました。原文が明確に別レコードだと判断できる場合は、訳の適用自体を見送り未解決のまま残すようにしました。
- 大文字・小文字だけが異なる原文を、別レコードとして誤って扱ってしまう不具合もあわせて修正しました。

### v0.56.1 (2026-08-29 14:06)

#### 修正

- v0.56.0のクロスMOD翻訳資産対応で、真にローカライズされたソース（バニラ本体等）が上書きされるケースのほぼ全てに、本来不要な「要レビュー」警告が付いてしまう不具合を修正しました。

## v0.56.0 (2026-08-29 13:24)

#### 追加

- **クロスMOD翻訳資産の欠落に対応**: 別MOD（翻訳パッチ・後発の拡張MOD等）が同じレコードを上書きした際、他の寄与者が既に持っていた日本語訳を自動的に復元するようになりました。原文が変わっている可能性がある場合は、訳は適用しつつログに要レビュー警告を出します。

### v0.55.5 (2026-08-29 02:58)

#### 修正

- 真にローカライズされたプラグイン（`Strings/*.STRINGS`同梱）で、日本語版が用意されていないFULLフィールドが、翻訳候補から一切現れず消えてしまう不具合を修正しました。

### v0.55.4 (2026-08-29 02:25)

#### 修正

- バックスラッシュの直後に`n`/`t`が続くテキスト（Windowsパスなど）が、翻訳データの保存・読み込み時に改行/タブへ誤変換される不具合を修正しました。

### v0.55.3 (2026-08-28 23:25)

#### 修正

- 不正なデータを含むレコードの処理中に例外が発生した際、フェールセーフ処理をすり抜けてツールがクラッシュする可能性がある不具合を修正しました。

### v0.55.2 (2026-08-28 22:35)

#### 修正

- GUIの「MO2再読込＆初期化」ボタンが、確認ダイアログの説明（「初期状態に戻します」）に反して、既存の翻訳結果を消去せず保持してしまう不具合を修正しました。

### v0.55.1 (2026-08-27 21:57)

#### 修正

- GUIで人力編集した非日本語の値が、DSD生成時に日本語チェックへ誤って引っかかり除外されてしまう不具合を修正しました。

#### その他

- 自動テストスイートを新設しました。

## v0.55.0 (2026-08-27 20:52)

#### 追加

- 「翻訳状況を初期化」等の破壊的な初期化操作の直前に、対象プラグインの翻訳結果を自動バックアップする機能を追加しました。
- GUIに「xTranslator翻訳XMLインポートフォルダを開く」「DSD出力フォルダを開く」ボタンなど、複数の利便性向上を行いました。

#### 修正

- 不正なプラグインデータ読み込み時にCLIが未処理例外でクラッシュする不具合を修正しました。
- 配布パッケージを単一ファイル発行に変更し、Nexus Modsでの誤検知（検疫）対策としました。

### v0.54.1 (2026-08-24 21:44)

Nexus Mods・GitHubへの再公開のみ（コード変更なし）。

## v0.54.0 (2026-08-23 21:20)

#### 追加

- 生成AI（クラウド）・ローカルLLM翻訳の信頼性を向上しました（複数行の原文がバッチ処理で正しく解析されない問題の解消など）。
- 「翻訳実行」中に安全に中断できるキャンセル機能を追加しました。

#### 修正

- GUIの「再スキャン」実行時のクラッシュを修正しました。
- LLM一括翻訳の文字数上限設定がGUI再起動時に保持されない不具合を修正しました。

#### 変更

- バージョン管理方式を、フォルダごとにコピーする方式からgit/GitHubベースへ移行しました。
