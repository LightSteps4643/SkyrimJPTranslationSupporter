# 変更履歴 (Changelog)

このプロジェクトは[セマンティックバージョニング](https://semver.org/lang/ja/)に準拠しています。
v0.54.0でバージョン管理をgit/GitHub（[Releases](https://github.com/LightSteps4643/SkyrimJPTranslationSupporter/releases)・タグ）ベースに移行しており、本ファイルもそれ以降を対象としています。

それより前の履歴は開発メモ（非公開）に記録されています。

### v0.62.11 (2026-09-18)

#### 変更

- Interface翻訳の「MO2対象Interfaceフォルダ読込み＆初期化」ボタンの確認ダイアログに、「ファイル形式の制約上、既に翻訳済みでも一部の候補が未翻訳として再検出される場合がある」旨の注記を追加しました。Interface翻訳の最終出力ファイルには、プラグイン翻訳のDSDファイルと異なり本ツール自身の判断を記録する仕組みが無く、この事象自体を完全に解消することは現状難しいため、既知の制約として明示する形にしました。あわせて文言の重複・改行位置も整理しました。

### v0.62.10 (2026-09-18)

#### 修正

- プラグイン翻訳で、既存のDSD翻訳パッチとの整合性判定（カバレッジ判定）が、DSD自身の実際の優先順位ルール（ゲート条件となっているプラグイン自身のロードオーダー降順→フォルダ内ファイル名の降順）を再現しておらず、無関係な別MOD（例: アイコン置換系MOD）が偶然同じ対象を上書きしていた場合に、実際にはゲーム内で正しく翻訳が表示されているにもかかわらず「未翻訳」と誤判定することがある不具合を修正しました。
- 上記のカバレッジ判定で「既にDSDで翻訳済み」と判定された候補が、翻訳作業の対象一覧（`translations.tsv`）に一切含まれなくなる仕様だったため、対象文字列が全て翻訳済みのプラグインがGUIの一覧から完全に消え、選択して再翻訳作業を行うことができない不具合を修正しました。今後はカバー済みの候補も「解決済み」として一覧に残り、翻訳率と共に表示されます。

### v0.62.9 (2026-09-18)

#### 変更

- 翻訳詳細ウィンドウ（プラグイン翻訳・Interface翻訳どちらも）の`TranslationCheck`列の見出しを「品質チェック」から「日本語化チェック」に変更しました。この列は翻訳の自然さ・正確さ（品質）を判定するものではなく、半角英数字がどの程度残っているかを機械的にチェックするものであるため、より実態に即した名称にしました。

### v0.62.8 (2026-09-18)

#### 修正

- v0.62.7で追加した`mod_glossary.tsv`生成処理（プラグイン翻訳・Interface翻訳共通）で、検出された語句の中に大文字小文字だけが異なる表記（例:「Hotkey」「HotKey」）が両方含まれていると、書き出し処理が重複キー例外でクラッシュする不具合を修正しました（実データ: VioLens - A Killmove Mod SEで確認）。どちらが同じ語句かは断定できないため、両方とも別々の行として書き出されます。

### v0.62.7 (2026-09-18)

#### 修正

- Interface翻訳（SJPTS_InterfaceText）で、「MO2対象Interfaceフォルダ読込み＆初期化」（`detect`）を実行した時点ではまだ`mod_glossary.tsv`が作成されず、「翻訳実行」（LLM呼び出し）まで進まないとMOD固有文字列の候補を確認できない問題を修正しました。検出処理自体はLLMに依存しないため、`detect`の時点で作成するようにしました。

### v0.62.6 (2026-09-18)

#### 変更

- ⑤ローカルLLM・⑥生成AI翻訳へのプロンプトで、`Reference examples`（既訳の参考例）各行にあった`(source: ...)`という出典表記を削除しました。モデルの判断に出典情報を使うよう指示していた箇所はなく、純粋なトークンの無駄だったためです。

### v0.62.5 (2026-09-18)

#### 追加

- 翻訳詳細ウィンドウ（プラグイン翻訳・Interface翻訳どちらも）に、`TranslationCheck`列（v0.62.2で追加した品質分類）を表示するようにしました。背景色のグラデーションに加えて、具体的な分類名も確認できます。

#### 修正

- 「MOD固有文字列の注釈」ウィンドウで訳文を保存した際、実際には保存されているにもかかわらず「0件の変更を保存しました」と表示される不具合を修正しました。
- ⑤ローカルLLM・⑥生成AI翻訳へのプロンプトで、MOD固有の語句をユーザーが手動で確認・修正済みの場合（`mod_glossary.tsv`への記入等）に、その訳語がモデルに無視されてしまうことがある問題を修正しました。人が確認済みの訳語は、モデル自身の判断や他の参考情報よりも優先して使うよう、指示文を強化しました。

### v0.62.4 (2026-09-18)

#### 追加

- MODごとの語彙ヒントファイル`mod_glossary.tsv`をGUIから直接編集できるようにしました。プラグイン翻訳・Interface翻訳どちらの画面でも、一覧の「詳細を確認」の隣に「MOD固有文字列の注釈」ボタンが追加され、そのMODの語句候補一覧を開いて訳文を直接記入・保存できます。
- Interface翻訳（UIテキスト翻訳）側にも、MOD特有語句の自動検出とヒント機能（`mod_glossary.tsv`の生成、記入済みの訳を他の未翻訳候補へのヒントとして活用する仕組み）を追加しました。これまでプラグイン翻訳側にしかありませんでした。

### v0.62.3 (2026-09-18)

#### 追加

- 翻訳詳細ウィンドウで、`TranslationCheck`列（v0.62.2で追加）の値に応じて行の背景色を白〜赤のグラデーションで変えるようにしました。レビューが必要そうな翻訳ほど赤みが強く表示されます。

### v0.62.2 (2026-09-18)

#### 追加

- `translations.tsv`・`interface_translations.tsv`に新規列`TranslationCheck`を追加しました。⑤ローカルLLM・⑥生成AI翻訳が返した訳文について、半角英数字の混入度合いから「完全に日本語化されているか」「タグを含むか」「タグ以外の英語が混ざっているか」「半分以上英語のままか」「英語のままか」を機械的に分類し、レビューが必要そうな訳文を見分けやすくします（①〜④の解決手法や、人間が確認・編集済みの行は対象外・空欄のままです）。

### v0.62.1 (2026-09-17)

#### 修正

- 複数のMODが同じレコードを順に上書きしている際、既に日本語化された箇所を後から英語に戻ってしまった別のMODへ引き継ぐ仕組み（クロスMOD過去訳）について、日本語の翻訳パッチが2つ以上連続する場合に、本来の原文（英語）まで遡って照合できていなかった不具合を修正しました。従来はこの場合、原文との照合を諦めて確認なしでそのまま適用していたため、レコードが実際には別のアイテムに転用されていた場合でも誤って古い訳を適用してしまう可能性がありました。
- あわせて、原文の英語が一切見つからない（MOD自身が最初から日本語で追加したレコード）場合の訳文について、そのレコード自身への適用はそのまま維持しつつ、同じ英文を持つ他の無関係な候補にまで拡大適用されないよう見直しました。

### v0.62.0 (2026-09-17)

#### 追加

- MODごとの語彙ヒントファイル`mod_glossary.tsv`（各プラグインのTranslation出力フォルダ内）を追加しました。そのMOD内で繰り返し使われているのに、単語単体では①〜④で解決できても組み合わせ全体としては不自然な訳になってしまう複合語（例:「Light Greatsword」）を自動検出して候補として提示します。日本語列に訳を記入すると、同じMODの他の未翻訳候補への翻訳ヒントとして使われます（記入は任意、記入済みの内容は再生成時も保持されます）。各候補にはこのMOD内での出現回数（Count）と、MOD特有度合いを示すスコア（Score）を併記し、Score降順で並びます。

#### 修正

- 上記の検出処理で、"45th"のような数字と英字が混ざった表記から数字部分だけが消え、残った接尾辞（"th"等）が独立した単語のように混入してしまう不具合を修正しました。
- 書物・メモ系の候補に含まれる、ゲーム内の書式指定（フォント指定・ページ区切り等、実際に画面へ表示されないマークアップ）が誤って検出結果に混入してしまう問題を修正しました。
- 上記の検出処理を含む翻訳ファイル一括生成が、対象プラグイン数に比例して大幅に遅くなっていた（実測: 183プラグインで10秒→62秒）不具合を修正しました。

### v0.61.3 (2026-09-17 17:57)

#### 修正

- LLM翻訳（⑤ローカルLLM・⑥生成AI翻訳）のプロンプトに含める「参考例」について、同じ実例（同一の原文・訳文ペア）が出典違いで重複して複数行表示されてしまい、文字数予算を無駄に消費していた問題を修正しました。完全に同じ実例は1行にまとめます。
- あわせて、参考例の件数に上限が無かったため、長文・汎用的な候補で数百件以上の実例が拾われてしまい、他の候補の参考例予算を圧迫する（またはバッチに詰め込める候補数を大きく減らす）ケースがあった問題を修正しました。候補1件あたりの参考例を「件数」と「文字数の割合」の上限のいずれか先に達した方で打ち切るようにし、⑤ローカルLLM（実行コストが低く時間の融通が利くため、やや手厚め）と⑥生成AI翻訳（呼び出し回数の削減を優先し、やや控えめ）で別々の上限値を使うようにしました。

### v0.61.2 (2026-09-17 05:04)

#### 修正

- 別の場所で既に翻訳済みの文言を再利用する仕組み（同一レコードを複数MODが上書きしている場合の訳文引き継ぎ）において、`<Alias=...>`のようなスクリプト変数を含む文言の大文字小文字の違いを、これまで誤って無視していた不具合を修正しました。今後はこの種の変数部分は大文字小文字も含めて完全に一致する場合のみ訳文を再利用し、一致しない場合は安全のため再翻訳の対象とします。

### v0.61.1 (2026-09-17 04:46)

#### 修正

- LLM翻訳（ローカルLLM・生成AI翻訳）の応答に、本ツール内部で使用する制御用のタグ・マーカーが誤って残ったまま、翻訳結果として受理されてしまう不具合を修正しました。実際にごく一部の翻訳結果で、本来表示されるべきでない記号が混ざって出力されるケースが確認されていました。今後はこのような応答を検出した場合、翻訳失敗として扱い、次回の翻訳実行時に再度翻訳を試みます。

### v0.61.0 (2026-09-17 03:39)

#### 修正

- 翻訳を完了しDSDファイルを実際にMO2導入した状態で、ツールを再読込＆初期化すると、一部の候補が毎回「未翻訳」として検出され続けてしまう不具合を修正しました。モデルが「翻訳不要」と判断した場合や、人間が意図的に非日本語のまま確定させた場合など、本ツール自身が正当な理由で原文のまま出力していたケースが、再読込のたびに未翻訳候補として復活してしまっていました。

#### 追加

- DSD出力ファイルが、他modの翻訳パッチより優先して読み込まれやすくなるよう、ファイル名を調整しました（DSD自身の仕様を直接確認した上での対応です）。100%の保証ではありませんが、多くのケースで改善が見込めます。

#### 内部変更

- 本ツールが翻訳結果に付与する内部タグ（`translations.tsv`のNotes列）に、他modや他ツールのデータと確実に区別できるよう接頭辞を追加しました。ユーザー向けの動作に直接影響するものではありませんが、翻訳詳細画面のNotes列の表示が変わります。

### v0.60.8 (2026-09-17 00:54)

#### 修正

- プラグイン翻訳用・Interface翻訳用のimportフォルダを、製品ルート直下の`import/plugin`・`import/interface`に集約しました（旧`Translation/import`・`InterfaceText/Translation/import`から移動）。実際のMODデータベースでは両方の翻訳データがまとめて配布されるケースが多いため、共通の親フォルダの下にまとめました。
- Interface翻訳の最終出力先を、プラグイン翻訳と同じ`out`フォルダ直下（`out/Interface/Translations/`）に統合しました。1つの`out`フォルダをそのままMO2に導入するだけで、両方の翻訳が反映されるようになります。
  - この統合は実は2026-09-12時点で設計上は意図されていましたが、GUI側の配線が別の場所を指したままになっており、実際には一度も有効になっていませんでした。今回あわせて修正しています。
- Interface翻訳のimportフォルダのファイル判定が、拡張子を見ずファイル名の末尾だけで行われていたため、プラグイン翻訳用のXMLファイルと共有フォルダに設定した場合に誤って読み込んでしまう可能性があった不具合を修正しました。
- 「設定」画面で、フォルダ参照ダイアログを開いた際に、指定したフォルダの中身ではなく1つ上の階層が表示されてしまっていた表示を修正しました（MO2パス設定を含む、フォルダ参照ボタン全般に適用されます）。

#### 追加

- 「設定」画面に、プラグイン翻訳用・Interface翻訳用のimport/outフォルダをそれぞれ個別に任意の場所へ変更できる設定項目を追加しました（既定では上記の統合されたフォルダが使われます）。
- プラグイン翻訳のCLIに、importフォルダを指定する`--import=`引数を新規追加しました（従来は変更不可でした）。なお、outフォルダ（DSD出力先）は元々CLI側に指定手段がありましたが、GUIがこれまで一切使っていなかったため、今回あわせてGUI側の呼び出しを修正しました。

### v0.60.7 (2026-09-16 23:16)

#### 修正

- 同一MOD内既訳ヒント（v0.60.6で追加）の不具合をいくつか修正しました。
  - 人間が翻訳詳細ウィンドウで確認・修正した既訳が、誤ってヒントの対象から除外されていた不具合を修正しました。
  - ヒントの表示形式を、実測検証済みの「凡例＋番号」方式に修正しました。
  - 「Reference examples（参考例）」の合計文字数にも上限（バッチ1回あたりの文字数上限の25%）を新たに設けました。
  - 同じ既訳が「Reference examples」と「同一MOD内既訳ヒント」の両方に重複して表示されることがある不具合を修正しました。
  - 複数の既訳候補が同点スコアになった場合、信頼度の高いもの（人間による手動修正等）が優先して表示されるようになりました。
- ローカルLLM/生成AI翻訳のバッチ呼び出しを制御する内部の仕組みを見直しました。従来は「バッチ」（1回の呼び出し）と「ラウンド」（応答の打ち切り時の自動再送単位）という2段階の構造になっており、再送時に直前のバッチ自身の結果がすぐには反映されないという制限がありました。今回、呼び出しのたびに最新の状態を反映する単一の仕組みに統合し、この制限を解消しました。
  - 副次的な影響として、デバッグ用のプロンプト保存ファイル名が`prompt_{provider}_batch{N}_of_{総数}[_round{番号}].txt`から`prompt_{provider}_call{番号}.txt`に変わりました。
- プラグイン翻訳・Interface翻訳それぞれで別々に実装されていたバッチ処理の共通部分を1つに統合しました。この過程で、Interface翻訳側にあった以下の不具合も見つかり修正しました。
  - ログの見出しが、ローカルLLM実行時にも常に「生成AI翻訳」と誤表示されていた不具合
  - 内部リトライが発生した際の記録ログが出力されていなかった不具合
  - 自動翻訳が成功した場合のログが一切出力されていなかった不具合（失敗時のみ出力されていた）

#### その他

- GUIの複数画面で重複していたTSVエスケープ処理（原文・訳文中のタブ/改行/バックスラッシュを安全に読み書きするための変換）を1箇所に統合しました。ユーザー向けの動作に変化はありません。

### v0.60.6 (2026-09-15 17:09)

#### 修正

- ローカルLLM/生成AI翻訳のバッチが応答の出力トークン数上限で打ち切られ、未解決分だけを自動で再送する際（v0.60.3で対応済み）、再送前後で同じ単語の訳し方がぶれてしまう不具合を修正しました（例: 1回目のバッチでは「GreatSword」を「グレートソード」と訳したのに、2回目のバッチでは「偉大な剣」と訳してしまう）。
  - 今後は、再送のたびに「このセッション内でこのMODについて既に得られた翻訳結果」をプロンプトに追加のヒントとして含めるようになり、表記ゆれが起きにくくなります。1回目のバッチでは（まだ何も訳していないため）ヒントは付きません。
  - プラグイン翻訳・Interface翻訳の両方に対応しています。

### v0.60.5 (2026-09-15 16:38)

#### 修正

- プラグイン翻訳のプロンプトに添える「Reference examples（参考例）」の選び方を改善しました。
  - 従来は、コーパス中の短い名前のような文字列（アイテム名等）だけを候補とし、それ以外の文章っぽい記述は一律で除外していました。今後は文章であっても候補と内容的に関連していれば参考例として表示されるようになり、他のMODで既に翻訳済みの同一の呪文説明文・台詞等をそのまま活用できるケースが増えます。
  - 参考例の選定基準を、単語の一致数の単純な合算から、検索エンジンで実績のある標準的な手法（TF-IDF重み付け＋コサイン類似度）に変更しました。長い無関係な文章が、たまたま数語だけ共有しているという理由で参考例の上位に来てしまう不具合を修正しています。
  - 1件あたりの参考例が500文字を超える場合は候補から除外するようにしました（無関係な長文が偶然の単語一致だけで紛れ込むことを防ぐため）。
  - 参考例の件数は固定（旧来は最大5件）ではなく、内容的な関連性の高さに応じて自動的に決まるようになりました（関連性の低いものは表示されず、逆に本当に関連性が高いものは以前より多く表示され得ます）。
- ローカルLLM翻訳のバッチあたり文字数上限の既定値を、3,000文字（コマンドライン実行時）／6,000文字（GUI実行時）という食い違った状態から、6,000文字に統一しました。

### v0.60.4 (2026-09-13 21:11)

#### 修正

- ローカルLLM/生成AI翻訳（OpenAI互換API方式）の応答が出力トークン数の上限で打ち切られた（`finish_reason=length`）場合に、これまでは一度得られた結果を反映した上で手動で再実行する必要がありましたが、今後はその時点で未解決の候補だけを対象に自動で再送するようになりました（プラグイン翻訳・Interface翻訳の両方）。
  - 再送は「直前の呼び出しが打ち切りだった」かつ「直前の呼び出しで1件以上新たに解決できた」場合のみ行われ、進捗が無くなった時点で自動的に止まります（固定回数の上限はありません）。

### v0.60.3 (2026-09-13 20:48)

#### 修正

- ログ（`translation.log`/`interfacetext.log`等）に、英語ログモード（`SKYRIMJPSP_LOG_LANG=en`）でも日本語の単語が混ざって出力される不具合を修正しました。
- Interface翻訳で、原文（英語）が完全に空・空白のみ・記号のみ（翻訳しようがない文字列）の場合、いつまでも「未翻訳」として扱われ、「未翻訳のケースがあります」の警告が出続けてしまう不具合を修正しました。今後はこのようなキーは検出時点で「翻訳不要」として扱われます（出力される`*_japanese.txt`の内容自体は変わりません）。

### v0.60.2 (2026-09-13 18:58)

#### 修正

- 候補の原文（英語）の先頭・末尾に意味のある空白が含まれる場合、モデルが指示通り正確に応答しても翻訳されない不具合を修正しました。
  - プラグイン翻訳・Interface翻訳の両方に影響していました（再実行しても直らない再現性のある不具合でした）。
- 「未翻訳のケースがあります」という警告の表示条件・文言を、プラグイン翻訳・Interface翻訳で統一しました。
  - 従来プラグイン翻訳側は、AI翻訳が1件も解決できなかった場合のみ警告していましたが、一部だけ解決できた場合（打ち切り等）は未解決が残っていても警告されませんでした。今後は1件でも未解決が残っていれば警告するようになります。
  - 文言も「設定に問題がある可能性があります」という決めつけではなく、モデルの応答形式・出力トークン上限等、複数の原因を並列に例示する中立的な内容に変更しました。
- ローカルLLM/生成AI翻訳（OpenAI互換API方式）の応答が、サーバー側の出力トークン数の上限で打ち切られた場合に、その旨をログへ記録するようにしました（`translation.log`/`interfacetext.log`）。
  - 未翻訳が多く残る場合、この理由かどうかがログから直接判別できるようになります。再実行すると、その時点で未解決の候補だけで改めてバッチが組まれるため、通常は数回の再実行で解決します。

### v0.60.1 (2026-09-12 22:50)

#### 修正

- モデル応答の解釈に関する不具合を修正しました。
  - 独自タグの前後に「Target:」等の余分な文字が含まれる場合、まれに誤った候補に紐付けられることがありました（プラグイン翻訳・Interface翻訳の両方）。
- 解釈に失敗した際、失敗理由の分類をログ出力するようにしました。
  - より詳細なログとして、失敗した場合はバッチ単位の応答全文をトレースログへ記録するようにしました。
- 以下はいずれもベータ機能「Interface翻訳」に関する修正です。
  - MOD名の誤翻訳を修正しました。
    - 大文字小文字・スペースの違いにより、MOD名自身が誤って音訳・翻訳されてしまうことがありました。
  - 翻訳詳細画面の不具合を2件修正しました。
    - 保存件数が実際は保存されているのに「0件保存されました」と表示される不具合。
    - プラグイン翻訳側と同じ操作性に統一。
  - 最終出力先を、プラグイン翻訳と同じフォルダに統合しました。
- 一部のMODのアイテム名等に埋め込まれた`Interface\Translations`のキー参照（例:「$TNG_TCT」）を誤って翻訳してしまう不具合を修正しました。
- 「DSDファイル生成」ボタンの対象範囲を修正しました。
  - 選択に関わらず常に全プラグインを処理してしまっていましたが、選択中のプラグインのみを対象にするように修正しました。

### v0.60.0 (2026-09-12 11:52)

#### 追加

- `Interface\Translations`（MCM設定画面・HUD・字幕等で使われるルースファイル機構）を対象とした新しい翻訳機能「Interface翻訳」を追加しました（**ベータ機能**）。
  - MCMメニュー内の文言やUIの文字列などについて、翻訳ファイル（*_english.txt）が存在していれば日本語翻訳が可能となります。
  - 既存のプラグイン翻訳（ESP/ESMレコード）と独立した機能として、GUIの新しいタブ「Interface翻訳」から利用できます。
  - 対応MODの一覧表示・ローカルLLM／生成AI（クラウド）による自動翻訳・翻訳ファイルの出力を、既存のプラグイン翻訳と同じ3ステップ操作で行えます。
  - プラグイン翻訳と同様に、ユーザーが用意した翻訳ファイル（*_japanese.txt）を優先的に取り込むimportフォルダを備えています。
  - 以下については、現状ではInterface翻訳の対象外です。
    - `Interface\Translations`に含まれないファイル
    - `*_english.txt`以外のファイル
    - キー（`$`で始まる識別子）がない文言
    - MOD独自で管理しているデータファイル

#### 修正

- Interface翻訳タブのMOD一覧で、行ごとの「翻訳状況を初期化」ボタンを押すと処理エラーになる不具合を修正しました。
- 内部処理用のCLI（プラグイン翻訳）の実行ファイル名を`SkyrimJPStringPatcher.exe`から`SJPTS_InGameText.exe`に変更しました（Interface翻訳用CLIの`SJPTS_InterfaceText.exe`と対称的な名称にするため）。配布フォルダ内のサブフォルダ名も同様に変更しています。

### v0.59.7 (2026-09-07 18:14)

#### 修正

- スキル説明文（`AVIF DESC`、鍛冶・破壊等）が翻訳候補として一切スキャンされていなかった不具合を修正しました。DSD自体は元々このタイプに対応しており、本ツール側のスキャン漏れが原因でした。

### v0.59.6 (2026-09-06 20:01)

#### 変更

- ローカルLLM翻訳・生成AI翻訳へのプロンプトから、綴りベースの機械的なカタカナ音訳ヒント（"Machine transliteration"）を削除しました。英語の綴りをそのまま変換するだけで発音とズレやすく、実質的にほぼ効果が無くなっていたためです。既存のコーパスに実例がある語はこれまで通り別の仕組み（単語単位の完全一致ヒント）で提示され、実例が無い未知の固有名詞はモデル自身の判断に委ねる形になります。

### v0.59.5 (2026-09-06 19:28)

#### 追加

- ツールのウィンドウタイトルに、実行中のバージョンを表示するようにしました（例: 「Skyrim JP Translation Supporter v0.60.0」）。
- 「翻訳実行」中、実行ログウィンドウのキャンセルボタン右側に、選択プラグイン全体に対する進捗（未翻訳文字数ベース、プラグイン単位で更新）をプログレスバーで表示するようにしました。
- 実行ログウィンドウの各行に時刻を表示するようにし、CLIの実行（MO2再読込＆初期化・翻訳実行・DSDファイル生成等）ごとに空行を挟んで区切るようにしました。

#### 変更

- ローカルLLM翻訳・生成AI翻訳（クラウド）で、モデルの応答からツールが候補を解決できなかった場合の理由を、実行ログウィンドウにその場で分かりやすく表示するようにしました（従来はログファイルにのみ記録され、実行中は気づけませんでした）。
- 「MO2再読込＆初期化」実行時、既存のバックアップ後に`Translation/out_temp`フォルダを一旦削除してから再構築するようにしました。今回のスキャンで対象外になったプラグインの古い翻訳データが削除されずに残り、プラグイン一覧に実態と合わない未翻訳件数が表示され続けてしまう不具合の修正です。確認ダイアログの文言も、自動バックアップが行われる旨がわかるように調整しました。

### v0.59.4 (2026-09-06 15:02)

#### 変更

- 各種ログファイル（`*.log`/`*.trace.log`）冒頭のバージョン表記を削除しました。v0.54.0のgit/GitHub tagベースの運用移行後、内部的に更新が追いつかず実際のバージョンとずれた表記のまま残っていたためです。

### v0.59.3 (2026-09-06 14:54)

#### 修正

- 自動翻訳（バニラコーパス完全一致・意味翻訳・音訳分解・簡易名前解決）で解決された訳文に日本語が含まれていなかった場合、DSDファイルへの出力から除外されてしまう不具合を修正しました。該当する候補は、原文のまま画面に取り残されるより出力しておいたほうが安全なため、除外せず警告ログを残した上でそのまま出力するようにしました（手動編集・ローカルLLM/生成AI翻訳の同種のケースと同じ扱いに統一）。

### v0.59.2 (2026-09-03 15:07)

#### 修正

- 本（BOOK）レコードで、本文（body）と短い説明文（description）が入れ替わってDSDファイルに出力されてしまう不具合を修正しました。本系のアイテムを追加するMODで、アイテムの説明文と本文が入れ替わって表示される原因になっていました（[Issue #2](https://github.com/LightSteps4643/SkyrimJPTranslationSupporter/issues/2)でご報告いただきました、ありがとうございました）。
  - 併せて、他の16種類のレコード種別・7種類のネスト構造については、同様の入れ替わりが無いことも確認済みです。

### v0.59.1 (2026-09-02 02:42)

#### 変更

- 「簡易名前解決」の既定をOFFに変更しました（当面はローカルLLM翻訳の方が高品質なため）。

#### 修正

- 翻訳詳細ウィンドウで、複数行の訳文セルを編集モードにすると改行が消えて見える不具合を修正しました（保存されるファイル自体は元々正しく、表示だけの問題でした）。
- ローカルLLM翻訳・生成AI翻訳で、モデルが訳文を区切りタグ（`<SJPTS_TARGET>`）で囲んで返した場合に、そのタグが訳文にそのまま残ってしまう不具合を修正しました。

## v0.59.0 (2026-09-01 22:58)

#### 追加

- ローカルLLM翻訳に、モデルの「思考」モードを切り替えるチェックボックスを追加しました（既定でチェック済み＝思考モードOFFが既定で有効）。
  - 思考モードは、対応モデルではONのままだと翻訳に大幅に時間がかかったり失敗しやすくなったりするため、OFF推奨です。
  - Ollama＋`gemma4:26b`の組み合わせで、実際に思考モードを切り替えられることを確認済みです。
  - 思考に対応していない`gemma3:12b`・`qwen2.5:14b-instruct`でも、この設定を送って害が無いことは確認済みですが、それ以外のモデル・サーバーとの組み合わせは未検証です。
- ローカルLLM翻訳・生成AI翻訳（クラウド）の「1回あたりの文字数上限」を、設定ウィンドウでそれぞれ独立して設定できるようにしました。
- 未翻訳候補の抽出時に除外した項目の理由を、専用ログファイル（`PickUpTarget/excluded_candidates.txt`）にまとめて出力するようにしました。あわせて、なぜ除外したのかが一目で分かるよう理由の文言も見直しています。

#### 変更

- 「生成AI連携設定」を独立ウィンドウから設定ウィンドウ内へ統合しました。
- ローカルLLM翻訳・生成AI翻訳のバッチ処理を全体的に見直し、翻訳できない結果を取りこぼしにくくしました。
- ローカルLLM翻訳・生成AI翻訳で、モデルの訳文に日本語が含まれない場合、以前は一律「翻訳失敗」扱いでしたが、意図的に翻訳していない文字列（スクランブル文字列等）である可能性も考慮し、専用タグを付けた上でDSDファイルにもそのまま出力し、後からレビューできるようにしました。

#### 修正

- ローカルLLM翻訳・生成AI翻訳で、翻訳自体は正しいのに未解決のまま残ってしまうことがある不具合を修正しました。
- ローカルLLM翻訳のタイムアウト・連続失敗時の打ち切りを見直し、応答が長くかかるモデルでも早期に諦めすぎないようにしました。
- ウィンドウ下部のボタン列に、操作を繰り返すうちに余分な空白ができてしまうことがある表示不具合を修正しました。

## v0.58.0 (2026-08-31 12:51)

#### 変更

- 配布フォルダの構成を変更しました。起動用の実行ファイル（`SkyrimJPStringPatcherGui.exe`）がフォルダ直下に配置され、直接ダブルクリックで起動できるようになりました（ランチャーの`.bat`ファイルは廃止）。内部処理用のCLI（`SkyrimJPStringPatcher.exe`）は`SkyrimJPStringPatcher`サブフォルダに移動しました。

### v0.57.4 (2026-08-30 15:04)

#### 変更

- 設定ウィンドウのmods/profile/overwrite個別パス設定を分かりやすく整理しました（説明文付きの枠にまとめ、項目名を簡潔化）。
- 設定ウィンドウの「MO2フォルダをロード」ボタンを削除しました（実行しても翻訳状況の一覧には反映されず、混乱を招く恐れがあったため）。MO2の再読み込みはベース画面の「MO2再読込＆初期化」をご利用ください。

#### 修正

- プロファイルフォルダを個別設定した際、実行ログの「プロファイル」表示がMO2側で現在選択中のプロファイル名のままになり、実際に処理したプロファイルと食い違って表示される不具合を修正しました。
- DSDファイル生成の出力ファイル名に実行日時を含めるようにしました。未翻訳分を後日追加で翻訳して再度DSD出力・導入した際、以前の翻訳がMO2の仮想ファイルシステム上で消えてしまう不具合を修正しています。

### v0.57.1 (2026-08-30 13:36)

#### 追加

- MO2の「Paths」設定タブでmods・profiles・overwriteフォルダを標準位置から再配置している場合に対応する、任意の3つのパス上書き設定を追加しました（設定ウィンドウ・CLIとも）。未設定（既定）であれば従来通りMO2インスタンスフォルダから自動的に導出されます。

#### 変更

- 設定ウィンドウのボタンを、単一の「設定を保存」から、一般的なOK（保存して閉じる）／キャンセル（保存せず閉じる）に変更しました。

#### 修正

- MO2インスタンスフォルダ、またはmods/プロファイル/overwriteフォルダのパスが誤っている場合に、原因の分からないクラッシュ（終了コード-532462766）で処理が止まってしまう不具合を修正しました。今後は分かりやすいエラーメッセージとともに、正常に処理を終了します。

### v0.56.2 (2026-08-29 14:39)

#### 修正

- v0.56.0のクロスMOD翻訳資産対応で、バニラのレコードを別物へ改名するオーバーホールMOD（例: パークツリー改変MOD）に対して、古い訳が無条件に適用されてしまう不具合を修正しました。原文が明確に別レコードだと判断できる場合は、訳の適用自体を見送り未解決のまま残すようにしました。
- 大文字・小文字だけが異なる原文を、別レコードとして誤って扱ってしまう不具合もあわせて修正しました。

### v0.56.1 (2026-08-29 14:06)

#### 修正

- v0.56.0のクロスMOD翻訳資産対応で、真にローカライズされたソース（バニラ本体等）が上書きされるケースのほぼ全てに、本来不要な「要レビュー」警告が付いてしまう不具合を修正しました。

## v0.56.0 (2026-08-29 13:24)

#### 追加

- **クロスMOD翻訳資産の欠落に対応**: 別MOD（翻訳パッチ・後発の拡張MOD等）が同じレコードを上書きした際、他の寄与者が既に持っていた日本語訳を自動的に復元するようになりました。原文が変わっている可能性がある場合は、訳は適用しつつログに要レビュー警告を出します。

### v0.55.5 (2026-08-29 02:58)

#### 修正

- 真にローカライズされたプラグイン（`Strings/*.STRINGS`同梱）で、日本語版が用意されていないFULLフィールドが、翻訳候補から一切現れず消えてしまう不具合を修正しました。

### v0.55.4 (2026-08-29 02:25)

#### 修正

- バックスラッシュの直後に`n`/`t`が続くテキスト（Windowsパスなど）が、翻訳データの保存・読み込み時に改行/タブへ誤変換される不具合を修正しました。

### v0.55.3 (2026-08-28 23:25)

#### 修正

- 不正なデータを含むレコードの処理中に例外が発生した際、フェールセーフ処理をすり抜けてツールがクラッシュする可能性がある不具合を修正しました。

### v0.55.2 (2026-08-28 22:35)

#### 修正

- GUIの「MO2再読込＆初期化」ボタンが、確認ダイアログの説明（「初期状態に戻します」）に反して、既存の翻訳結果を消去せず保持してしまう不具合を修正しました。

### v0.55.1 (2026-08-27 21:57)

#### 修正

- GUIで人力編集した非日本語の値が、DSD生成時に日本語チェックへ誤って引っかかり除外されてしまう不具合を修正しました。

#### その他

- 自動テストスイートを新設しました。

## v0.55.0 (2026-08-27 20:52)

#### 追加

- 「翻訳状況を初期化」等の破壊的な初期化操作の直前に、対象プラグインの翻訳結果を自動バックアップする機能を追加しました。
- GUIに「xTranslator翻訳XMLインポートフォルダを開く」「DSD出力フォルダを開く」ボタンなど、複数の利便性向上を行いました。

#### 修正

- 不正なプラグインデータ読み込み時にCLIが未処理例外でクラッシュする不具合を修正しました。
- 配布パッケージを単一ファイル発行に変更し、Nexus Modsでの誤検知（検疫）対策としました。

### v0.54.1 (2026-08-24 21:44)

Nexus Mods・GitHubへの再公開のみ（コード変更なし）。

## v0.54.0 (2026-08-23 21:20)

#### 追加

- 生成AI（クラウド）・ローカルLLM翻訳の信頼性を向上しました（複数行の原文がバッチ処理で正しく解析されない問題の解消など）。
- 「翻訳実行」中に安全に中断できるキャンセル機能を追加しました。

#### 修正

- GUIの「再スキャン」実行時のクラッシュを修正しました。
- LLM一括翻訳の文字数上限設定がGUI再起動時に保持されない不具合を修正しました。

#### 変更

- バージョン管理方式を、フォルダごとにコピーする方式からgit/GitHubベースへ移行しました。
